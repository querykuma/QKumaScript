print "# test - fib";
println;

var fib = function(x) {
    if (x < 2) {
        return x;
    } else {
        return fib(x-1) + fib(x-2);
    }
};

var ret = fib(18);

print "ret = ";
print ret;
println;
